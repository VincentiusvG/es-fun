const test = require('tape-catch');
const { esfun } = require('../src/es-fun.js');

// pipe/bind/map one parameter
// pipe/bind/map more parameters

test('Matching on union types', function (t) {
  //...

  t.end();
});